language: elixir
sudo: false
elixir: 1.2.3
notifications:
  recipients:
    - ben.wilson@cargosense.com
otp_release:
  - 18.3
script: "MIX_ENV=test mix local.hex --force && MIX_ENV=test mix do deps.get, test"
